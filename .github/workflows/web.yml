name: Web

on:
  push:
    branches: [main]
    paths: ['web/**', 'contracts/**', '.github/workflows/web.yml']
  pull_request:
    branches: [main]
    paths: ['web/**', 'contracts/**', '.github/workflows/web.yml']

concurrency:
  group: web-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: web

jobs:
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '26'
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test

      - name: Check Mosaic GraphQL contract (issue #16)
        working-directory: .
        run: node contracts/check-contract.mjs --introspection web/src/data/testing/realIntrospection.json

      - name: Build
        run: npm run build
